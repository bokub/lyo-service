{
  "name": "${publishName}",
  "description": "Browser-compatible version of ${name}",
  "version": "${version}",
  "license": "${typeof(license) === 'undefined' ? 'unknown' : license}",
  "main": "${file}"
}
